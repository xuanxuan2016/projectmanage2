(function(Vue, window, document) {
    /**
     * 业务对象
     */
    var home = {
    };

    /**
     * vue对象
     */
    var app = new Vue({
        el: '#app',
        data: {
        },
        created: function() {
        },
        methods: {
        },
        mounted: function() {
            this.$nextTick(function() {
            });
        }
    });

}(Vue, window, document));


